class Event{
  final String title;
  final String description;
  final DateTime startTime;
  final DateTime endTime;
  final DateTime date;
  Event(this.title, this.description, this.startTime, this.endTime, this.date);
}